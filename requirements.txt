# Lantern — Python dependencies
#
# Grows one module at a time. Each section below is added when its module is built.
# When you add a dep, also note (a) which module needs it and (b) why.

# --- Shared / configuration (every module uses these) ---------------------
python-dotenv>=1.0.0          # load .env at runtime
pyyaml>=6.0                   # parse channels/*.yaml
pydantic>=2.0                 # validate channel + env config schemas
click>=8.1                    # CLI framework

# --- Module 1: research.py -------------------------------------------------
google-api-python-client>=2.100   # YouTube Data API v3 client
pytrends>=4.9                     # Google Trends (unofficial scraper; defended with try/except)

# --- Module 3: voice.py ----------------------------------------------------
edge-tts>=6.1                     # Microsoft Edge TTS (free, no API key required)

# --- Module 4: assemble.py -------------------------------------------------
moviepy>=2.1                      # video composition (ffmpeg wrapper)
Pillow>=10.0                      # thumbnail rendering

# --- Module 5: dashboard (FastAPI) -----------------------------------------
fastapi>=0.110                    # web framework for review dashboard
uvicorn[standard]>=0.29           # ASGI server
jinja2>=3.1                       # template engine
python-multipart>=0.0.9           # form parsing for POST endpoints

# --- Module 6: upload.py ---------------------------------------------------
google-auth-oauthlib>=1.2         # one-time browser OAuth flow for YouTube upload

# --- v1.1: captions post-processing for assemble.py -----------------------
faster-whisper>=1.0               # local STT for caption timestamps; ~150MB model on first use
