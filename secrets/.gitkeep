# Place the YouTube OAuth client JSON here as `secrets/client_secret.json`
# (we'll set this up before module 6 / upload.py).
#
# Everything in this directory is gitignored except this .gitkeep file —
# nothing under secrets/ ever enters git history.
